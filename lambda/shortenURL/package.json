{
  "name": "tgs-shortenURL",
  "type": "module",
  "scripts": {
    "build:lambda": "rm -rf ../dist && mkdir -p ../dist && zip -r ../dist/shortenURL.zip index.mjs node_modules package.json"
  },
  "dependencies": {
    "ioredis": "^5.3.2"
  }
}